# Runtime split (disabled)

This directory reserves the future RID-specific runtime packaging boundary. M0 never produces a runtime package. `eng/pack.ps1 -Runtime` fails until the policy and a complete manifest explicitly enable it.

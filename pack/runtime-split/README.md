# Runtime split (disabled)

This directory reserves the future RID-specific runtime packaging boundary. M1 does not produce a runtime package. `eng/pack.ps1 -Runtime` fails until the policy and a complete, licensed, runtime-verified manifest explicitly enable it.

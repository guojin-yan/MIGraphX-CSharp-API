# 0.9.0-rc.9 local prerelease candidate

`0.9.0-rc.9` is an unpublished M11 candidate. It keeps the core public surface at 27 types and 160 members, the adapter at 3 types and 11 members, and the aggregate API map at 84 supported, 107 planned, and 1 unsupported.

This candidate adds package-only execution entry points for separately authorized native isolation, timing samples, and bounded long-run records. Runner output remains `runtime-candidate-executed-review-required`; external evidence must be independently reviewed before any case is promoted to `runtime-executed`.

The candidate does not publish NuGet packages, create a tag or release, deploy Pages, run Actions, modify the system ROCm installation, or claim Windows MIGraphX support.

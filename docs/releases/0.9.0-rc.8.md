# 0.9.0-rc.8 local prerelease candidate

This unpublished M11 candidate adds no public API. Core remains `27 types / 160 members`, adapter remains `3 / 11`, and the aggregate C API map remains `84 supported / 107 planned / 1 unsupported`.

Rc.8 preserves the rc.7 dynamic native-shape snapshot repair and changes only SDK selection: `global.json` now requests .NET SDK `10.0.110` with `latestFeature` roll-forward. This permits the approved host's installed SDK while allowing newer local feature bands to rebuild the same candidate.

The candidate remains `runtime-deferred`. It requires a fresh source SHA, freshly built exact core/adapter/HipSharp package identities, clean detached source, and bounded official preflight before functional execution. No long-run, timing, publication, tag, release, Pages deployment, Actions run, or NuGet publication is authorized by this source change.

Chinese summary: `0.9.0-rc.8` 不新增公开 API。它保留 rc.7 的动态 native shape 快照修复，并将 `global.json` SDK 基线改为 .NET `10.0.110` 与 `latestFeature`；在新的源码、三包身份和预检绑定前，不提升任何官方运行时结论。

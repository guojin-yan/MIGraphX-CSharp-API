# 0.9.0-rc.3 local prerelease candidate

This unpublished candidate adds no public API. Core remains `27 types / 160 members`, adapter remains `3 / 11`, and the fixed aggregate C API map remains `84 supported / 107 planned / 1 unsupported`.

M11 synchronizes the independently reviewed M10 post-build external evidence without rewriting the historical `0.9.0-rc.2` package. It adds deterministic project-owned Identity, ordered Identity+Neg multi-output, and dynamic Identity fixtures; a machine-readable M4-M6 case and threshold matrix; an exact-package-only official runtime probe; a separate review script; and an explicit Windows native policy.

The candidate is `runtime-deferred`. No `0.9.0-rc.3` official host or time-window authorization exists, so M4-M6 bounded functional, official isolation negatives, cache restart, long-run, and timing results are not claimed. The long-run and timing layer requires separate Owner authorization. AMD's fixed MIGraphX 2.15.0 documentation applies to Linux and the Windows HIP SDK component table marks AI libraries unavailable, so the fixed-version Windows MIGraphX provider is `not-applicable` rather than indefinitely pending.

The final candidate must regenerate core, adapter, exact HipSharp dependency, ZIP and normalized identities, SBOM, local unsigned provenance, and vulnerability evidence against the final pushed SHA. It remains managed-only and unpublished; no tag, release, Pages deployment, or Actions run is authorized.

中文摘要：`0.9.0-rc.3` 不新增公开 API，只加入 M10 外部证据同步、M11 fixture/case/threshold、package-only probe、独立 review 与 Windows 固定版本策略。没有新的官方授权，M4-M6、负向隔离、长跑和计时仍未执行，候选未发布。

# 0.0.0 engineering baseline

This local candidate establishes the M0 repository, exact 15-TFM core build, package audit, representative clean consumers, upstream manifests, DocFX, CI workflows, and cloud-recording method.

It is not a published release and contains no native binding, native library, ONNX execution, asynchronous execution, runtime package, or AMD GPU validation. Licensing remains pending.

# Runtime manifests

No runtime asset manifest exists in M0. A future RID manifest must satisfy `schema.json`, include every native file and transitive dependency, record license and hashes, and keep build/package/runtime statuses independent.
